from django.urls import path, include
from . import views

app_name = 'accounts'

urlpatterns = [
    # path('view/', views.UsersListView.as_view(), name='users_list'),
    # path('api/details/', views.UserDetailAPI.as_view(), name='user_details_api'),
    # path('detail/<int:user_id>/', views.UserDetailView.as_view(), name='user_detail'),
    # path('post/redir/', views.UserPostDetail.as_view(), name='user_post_detail'),

]
